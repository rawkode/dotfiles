managers:
  arch:
    command: yay
    install: -Sy --noconfirm --needed
    uninstall: -Rd --noconfirm
  debian:
    command: apt
    install: install -y
    uninstall: uninstall -y
  nix:
    command: nix-env
    install: -i
    uninstall: -e

packages:
  # CLI, Shells, and Terminals
  - dist:
      arch:
        - alacritty
        - bat
        - exa
        - jq
        - keybase
        - kbfs
        - ripgrep
        - skim
        - zsh

  # Desktop
  - name: i3
    dist:
      arch:
        - arandr
        - compton
        - i3-gaps
        - i3lock
        - nitrogen
        - pasystray
        - polybar
        - rofi
        - rofi-emoji

  - name: Gnome / GTK Extensions & Themes
    dist:
      arch:
        - arc-gtk-theme
        - bibata-cursor-translucent
        - chrome-gnome-shell
        - vimix-gtk-theme

  - name: fonts
    dist:
      arch:
        - noto-fonts-emoji
        - ttf-cascadia-code
        - ttf-google-fonts-typewolf

  # Web Browsers
  - name: google-chrome

  # Communications
  - dist:
      arch:
        - discord
        - slack-desktop
        - zoom

  # Yubikey
  - dist:
      arch:
        - ccid
        - gnupg
        - pcsclite
        - pcsc-tools
        - yubikey-personalization

  # Development
  - name: Editors & Tooling
    dist:
      arch:
        - github-cli
        - visual-studio-code-bin

  - name: Crystal
    dist:
      arch:
        - crystal
        - shards

  - name: Containers
    dist:
      arch:
        - buildah
        - crun
        - docker
        - podman
        - skopeo

  - name: Google Cloud
    dist:
      arch:
        - google-cloud-sdk

  - name: jsonnet

  - name: Kubernetes
    dist:
      arch:
        - kind-bin
        - kubectl
        - kubernetes-helm-bin
        - minikube

  - name: kvm2 (for minikube)
    dist:
      arch:
        - gnome-boxes!
        - qemu!
        - dnsmasq
        - ebtables
        - libvirt
        - qemu-headless

  - name: Scala
    dist:
      arch:
        - sbt
        - scala
        - scalafmt

scripts:
  - curl https://keybase.io/rawkode/pgp_keys.asc | gpg --import
  - sudo systemctl start pcscd.service
  - sudo systemctl start pcscd.socket
  - sudo usermod -a -G libvirt $(whoami)
  - sudo systemctl enable libvirtd virtlogd
  - sudo systemctl start libvirtd virtlogd
  - sudo virsh net-autostart default
  - sudo virsh net-start default
  - keybase config set mountdir ${HOME}/Keybase
  - keybase ctl autostart --enable
  - mkdir -p ${HOME}/Code/src/{aur.arhclinux.org,bitbucket.org,github.com,gitlab.com}
  - mkdir -p ${HOME}/Pictures/Wallpapers
  - curl https://rawkode.keybase.pub/pictures/wallpapers/Stargate/Stargate%205.jpg -o ${HOME}/Pictures/Wallpapers/Stargate.jpg
  - sudo usermod -a -G docker $(whoami)
  - sudo systemctl enable docker
  - sudo systemctl start docker
